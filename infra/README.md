# Infrastructure

Deployment automation will live here after the first local validation. It will create only lab-scoped resources in `us-east-2`:

- Artifact S3 bucket
- Least-privilege MicroVM image build role
- Lambda MicroVM image
- Log retention configuration

No AWS resources are created by the repository scaffold.
