{
  "name": "lambda-microvm-dungeon-agent",
  "version": "0.1.0",
  "private": true,
  "description": "A stateful AI dungeon-agent lab for AWS Lambda MicroVMs",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/state-store.js && npm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DavidCs9/lambda-microvm-dungeon-agent.git"
  },
  "license": "MIT"
}
